2026-04-15 — Task 05 (Single-Page Clear + Confirmation UI): Wired full single-page clear flow.
  - clear-runner.ts: added getTotalCount() (reads span.total-count) and clearCurrentPage(total, onProgress)
  - App.tsx: three-state UI (idle → running → done); confirmation Modal with article count; live "Removing… N/total" counter; 3s done message then reset to idle

2026-04-15 — Task 04 (Single Row Removal TDD): Implemented removeRow() in clear-runner.ts.
  - Sets .amount-input to 999, clicks .btn-danger, waits via MutationObserver for row removal
  - Resolves immediately if row already absent
  - 4 new tests (total 12 passing)

2026-04-15 — Task 03 (State Management TDD): Implemented startClearing, cancelClearing, resumeIfActive in clear-runner.ts.
  - ClearOffersState interface + STORAGE_KEY constant
  - startClearing(total) writes { active: true, removed: 0, total } to chrome.storage.local
  - cancelClearing() removes clearOffersState key
  - resumeIfActive() returns stored state or null
  - 4 new tests (total 8 passing)

2026-04-15 — Task 02 (Row Scraping TDD): Implemented getArticleRows() in clear-runner.ts with 4 passing tests.
  - src/entrypoints/clearOffers.content/clear-runner.ts: getArticleRows() queries div[id^="articleRow"] and returns IDs
  - src/entrypoints/clearOffers.content/__tests__/clear-runner.test.ts: 4 tests covering empty, single, multiple, and filtering

2026-04-15 — Task 01 (Content Script Scaffold): Created clearOffers.content entrypoint.
  - src/entrypoints/clearOffers.content/index.tsx: content script matching *://*.cardmarket.com/*/*/Stock/Offers/Singles*, mounts React into the top pagination row's last col-3
  - src/entrypoints/clearOffers.content/App.tsx: "Clear All Offers" btn-danger button, enabled/disabled based on presence of articleRow elements via MutationObserver
  - No removal logic yet (placeholder console.log on click)
